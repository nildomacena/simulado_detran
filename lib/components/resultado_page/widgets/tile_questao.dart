import 'package:flutter/material.dart';

class TileQuestao extends StatelessWidget {
  const TileQuestao({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
